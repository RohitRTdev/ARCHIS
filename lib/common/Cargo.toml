[package]
name = "common"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["lib"]

[dependencies]
num-traits = { version = "*", default-features = false }

[features]
acpi = []
test-kernel = []